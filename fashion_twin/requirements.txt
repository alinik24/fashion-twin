# ─── Core ────────────────────────────────────────────────────────────────────
pydantic-settings>=2.0
python-dotenv>=1.0

# ─── LLM — Azure OpenAI (gpt-5.x, gpt-4.1, embeddings) ─────────────────────
openai>=1.30                  # Azure OpenAI SDK (compatible with azure_se, azure_weu)

# ─── Data collection ─────────────────────────────────────────────────────────
vinted-scraper>=2.2           # vinted_scraper package
playwright>=1.40              # headless browser for Vestiaire + trend scraping
httpx>=0.27                   # async HTTP client

# ─── Embeddings ──────────────────────────────────────────────────────────────
fashion-clip>=0.0.2           # FashionCLIP image embeddings
torch>=2.0                    # FashionCLIP backend (CPU or CUDA)
transformers>=4.35
Pillow>=10.0

# ─── Storage ─────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9          # PostgreSQL driver
qdrant-client>=1.7            # Qdrant vector DB
redis>=5.0                    # Redis for caching and price tracking

# ─── Recommendation / ML ─────────────────────────────────────────────────────
# OPTIONAL: The following require C++ build tools (Visual Studio) on Windows
# lightfm>=1.17               # hybrid collaborative filter (WARP loss)
# implicit>=0.7.2             # ALS collaborative filtering
# contextualbandits>=0.10     # online LinUCB contextual bandit

# Core ML packages (work without C++ build tools)
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10

# ─── Utilities ───────────────────────────────────────────────────────────────
requests>=2.31                # image download in embedder

# ─── Dev / test ──────────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.23
ruff>=0.1
mypy>=1.6
